# Runtime dependencies actually imported by the app.
#
# Removed: torch, transformers, sentence-transformers, langchain, langgraph,
# pinecone-client, sqlalchemy, asyncpg, redis. Nothing in src/ or config/
# imports them and together they added roughly 1.3 GB, which blows past the
# 250 MB serverless bundle limit.

# Core framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Document extraction
pypdf>=5.0.0
pdfplumber>=0.11.0

# LLM provider
# Only the configured provider's SDK is needed: src/llm loads provider modules
# lazily. Add `anthropic` or `openai` here if you switch LLM__PROVIDER.
google-genai>=1.0.0

# Vector store
# Qdrant replaces ChromaDB for serverless deployment: the chromadb wheel pulls
# in onnxruntime/kubernetes/grpc (~356 MB) and needs a writable disk, neither of
# which works on Vercel. qdrant-client is ~2 MB and talks to a hosted cluster.
qdrant-client>=1.7.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
httpx>=0.26.0
tenacity>=8.2.0
python-json-logger>=2.0.7
